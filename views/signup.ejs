<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bệnh viện DHA</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <link href="css/authens/signup.css" rel="stylesheet">
    <link rel="icon" href="/images/icon.png">
</head>

<body>
    <div class="register-container">
        <h3 class="text-center mb-4">Tạo Tài Khoản</h3>

        <% if (errorMessage) { %>
            <div class="alert alert-danger" role="alert">
                <%= errorMessage %>
            </div>
        <% } %>

        <form method="POST" id="signupForm">
            <div class="mb-2">
                <label for="username" class="form-label">Tên đăng nhập</label>
                <input type="text" class="form-control" maxlength="50" id="username" name="username" placeholder="Nhập tên đăng nhập">
                <small class="text-danger d-none" id="usernameError">Vui lòng nhập tên đăng nhập.</small>
            </div>
            <div class="mb-2 position-relative">
                <label for="password" class="form-label">Mật khẩu</label>
                <div class="input-group">
                    <input type="password" class="form-control" maxlength="50" id="password" name="password" placeholder="Nhập mật khẩu">
                    <button type="button" class="btn btn-outline-secondary" id="togglePassword">
                        <i class="fas fa-eye"></i>
                    </button>
                </div>
                <small class="text-danger d-none" id="passwordError">Vui lòng nhập mật khẩu.</small>
            </div>
            <div class="mb-2">
                <label for="fullname" class="form-label">Họ và tên</label>
                <input type="text" class="form-control" maxlength="50" id="fullname" name="fullname" placeholder="Nhập họ và tên">
                <small class="text-danger d-none" id="fullnameError">Vui lòng nhập họ và tên.</small>
            </div>
            <div class="mb-2 form-inline">
                <div class="form-group">
                    <label for="birthdate" class="form-label">Ngày sinh</label>
                    <input type="date" class="form-control" id="birthdate" name="birthdate" onfocus="this.max = new Date().toISOString().split('T')[0]">
                    <small class="text-danger d-none" id="birthdateError">Vui lòng chọn ngày sinh.</small>
                </div>
                <div class="form-group">
                    <label for="gender" class="form-label">Giới tính</label>
                    <select class="form-select" id="gender" name="gender">
                        <option value="Nam">Nam</option>
                        <option value="Nữ">Nữ</option>
                    </select>
                </div>
            </div>
            <div class="mb-2">
                <label for="phone_number" class="form-label">Số điện thoại</label>
                <input type="text" class="form-control" maxlength="15" id="phone_number" name="phone_number" placeholder="Nhập số điện thoại">
                <small class="text-danger d-none" id="phoneError">Vui lòng nhập số điện thoại hợp lệ.</small>
            </div>
            <div class="mb-2">
                <label for="address" class="form-label">Địa chỉ</label>
                <input type="text" class="form-control" id="address" maxlength="100" name="address" placeholder="Nhập địa chỉ">
                <small class="text-danger d-none" id="addressError">Vui lòng nhập địa chỉ.</small>
            </div>
            <div class="mb-4">
                <label for="gmail" class="form-label">Gmail</label>
                <input type="email" class="form-control" id="gmail" maxlength="100" name="gmail" placeholder="Nhập gmail">
                <small class="text-danger d-none" id="emailError">Vui lòng nhập email hợp lệ.</small>
            </div>
            <button type="submit" class="btn btn-success w-100">Đăng ký</button>
        </form>        
        
        <div class="form-link mt-3">
            Bạn đã có tài khoản?
            <a href="/login">Đăng nhập</a>
        </div>
    </div>

    <script src="/js/signup.js" type="text/javascript" defer></script>
</body>
